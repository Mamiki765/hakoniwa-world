# 3.10.0 地下バランス再設計の依頼・判断記録

状態: 実装候補と自己検証の記録を作成済み。独立レビュー・実戦50勝基準群・本番反映は未実施。技・数値・比較結果と検証範囲は[実装候補の記録](3.10.0-underground-rebuild.md)、短い説明は[3ツリーの一覧](3.10.0-skill-list.md)を参照する。

## 依頼と比較の基準

- Ownerの直接依頼: Astra xhighの試験運用、release/3.10.0としてバランス調整、GitHubを使わない。
- 参照資料: `hakoniwa-underground-skill-tree-rebase-instructions-2026-09-13.md`。資料中の設計案や過去の承認記述と、この作業で得たOwner決定を区別する。
- 設計・比較・最終自己レビューの実行設定: このタスクのturn contextで `gpt-6-astra` / `xhigh` を確認。
- Forgejo main: `87452d06cd54e07dfde33f90e4174e5d37197045`。2026-09-13にfetchして固定。独立したcloneの `release/3.10.0` で作業する。
- 本番観測: 2026-09-13 05:35:41 UTCのMCP snapshotはRuleset v25 / ID 40、pending migration 0、DB/web healthy。checkout SHAは上記mainだが、deployed SHA・application versionはunknownであり、稼働imageとの厳密一致を証明していない。
- merge、deploy、本番DB変更、補填実行は対象外。
- 変更前の代表確認: PT combat / AIの31 tests、259 assertions、0.374秒 PASS。
- 本番の実戦50勝基準群は未取得。合成benchmarkと実戦snapshot再現を混同しない。

## 今回のOwner決定

1. 敵全体攻撃による覚醒ゲージは、実ダメージまたはバリアダメージを受けた各playerに、その敵actionにつき最大1回加算する。複数hitでも各人1回。
2. レンタル確定・更新時に、借用メンバー全員をHP満タン・覚醒0にする。その後のHP・覚醒は借り手側で通常戦闘間に引き継ぐ。
3. 宿屋は借用メンバーのHPも全回復する。覚醒は保持する。
4. HP満タン・覚醒0へのリセットは明示的なレンタル更新だけ。通常の再読込み・能力再計算・出発ではリセットしない。能力再計算時はHP割合維持を基本案とする（Ownerは現在値維持も許容）。
5. 物理はCT管理、魔法はMP管理の役割感を目指す。祝福には能動的なMP自給を用意するが、MPだけの系統にはしない。回復恩寵を大技の別軸とする。
6. 行動を消費しない物理技は、CT・控えめな威力・技枠を使うトレードオフの案。短剣職のイメージは武器種の取得・使用制限を意味しない。
7. 初期予算で攻撃魔法とヒールを取得できることを確認する。
8. 技が出揃ったら3ツリーの短い効果一覧を作る。提示された効果の例文は技の追加要求ではなく、一覧の作成は名前・倍率・効果・技数を固定することでもない。

## 実装で採用した扱い

旧版benchmarkを保存し、取得ツリー・MP経済・技巧の候補を比較した。同じcanonical combatへ技能・対象・AIを接続し、SP移行・レンタル状態・UIを実装した。保存済みbattle/idempotency、Lv/XP/STP/成長型/装備/攻略実績を維持する移行と、focused regression・統合確認・狭幅/広幅UI・release checkpointの結果を実装候補の記録にまとめた。

係数・ノード数・SP予算は比較を経た実装案であり、Owner固定仕様とは扱わない。handoffは変更していない。
